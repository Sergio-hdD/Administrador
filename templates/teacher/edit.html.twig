{% extends 'base.html.twig' %}

{% block title %}Edit Teacher{% endblock %}

{% block body %}
    <h1>Edit Teacher</h1>

    {% for flash_message in app.session.flashbag.get('massage') %}
        <div class="flash-danger">
            {{ flash_message }}
        </div>
    {% endfor %}
    
    {{ include('teacher/_edit_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_teacher_index') }}">back to list</a>

    {{ include('teacher/_delete_form.html.twig') }}
{% endblock %}
