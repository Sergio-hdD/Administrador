{{ form_start(form) }}
    {{ form_row(form.dni)}}
    {{ form_row(form.email)}}
    {{ form_row(form.lastname)}}
    {{ form_row(form.name)}}
    {{ form_row(form.password)}}
    {{ form_row(form.phone)}}
    {{ form_row(form.securityKey)}}
{{ form_end(form) }}