{% extends 'base.html.twig' %}

{% block title %}New Student{% endblock %}

{% block body %}

    {% for flash_message in app.session.flashbag.get('massage') %}
        <div class="flash-danger">
            {{ flash_message }}
        </div>
    {% endfor %}

    {% set content = include('student/_form.html.twig') %}
        {% include "card_content.html.twig" with {
        'appicon': 'fa-dashboard',
        'title': 'Create new Student.',
        'detalle':content,
        }
    %}
    
{% endblock %}
