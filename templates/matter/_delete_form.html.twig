<form method="post" action="{{ path('app_matter_delete', {'id': matter.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ matter.id) }}">
    <button class="btn">Delete</button>
</form>
