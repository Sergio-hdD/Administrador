{% extends 'base.html.twig' %}

{% block title %}TeacherExam{% endblock %}

{% block body %}
    <h1>TeacherExam</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ teacher_exam.id }}</td>
            </tr>
            <tr>
                <th>IsActive</th>
                <td>{{ teacher_exam.isActive ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_teacher_exam_index') }}">back to list</a>

    <a href="{{ path('app_teacher_exam_edit', {'id': teacher_exam.id}) }}">edit</a>

    {{ include('teacher_exam/_delete_form.html.twig') }}
{% endblock %}
