<form method="post" action="{{ path('app_teacher_exam_delete', {'id': teacher_exam.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ teacher_exam.id) }}">
    <button class="btn">Delete</button>
</form>
